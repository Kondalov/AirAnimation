<UserControl x:Class="AirAnimation.App.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">

    <UserControl.Resources>
        <Style TargetType="RadioButton" x:Key="MapStyleRadio">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush_}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BgCardHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource BgDeepBrush}">
        <!-- WebView2 hosting MapLibre -->
        <wv2:WebView2 x:Name="WebMap"
                      Source="about:blank"
                      DefaultBackgroundColor="Transparent"/>

        <!-- Map style switcher overlay -->
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top"
                    Margin="12,12,0,0" Orientation="Vertical">
            <Border Style="{StaticResource Card}" Padding="8">
                <StackPanel>
                    <TextBlock Text="Стиль карты" FontSize="10"
                               Foreground="{StaticResource TextMutedBrush}"
                               Margin="0,0,0,6"/>
                    <UniformGrid Columns="2" Width="180">
                        <RadioButton Content="🛰 Спутник" Tag="satellite" Style="{StaticResource MapStyleRadio}" GroupName="MiniMapStyle"
                                     IsChecked="{Binding CurrentStyleKey, Converter={StaticResource EqualityConverter}, ConverterParameter=satellite}" Click="StyleBtn_Click"/>
                        <RadioButton Content="🗺 Liberty 3D" Tag="liberty" Style="{StaticResource MapStyleRadio}" GroupName="MiniMapStyle"
                                     IsChecked="{Binding CurrentStyleKey, Converter={StaticResource EqualityConverter}, ConverterParameter=liberty}" Click="StyleBtn_Click"/>
                        <RadioButton Content="🎨 Яркая" Tag="bright" Style="{StaticResource MapStyleRadio}" GroupName="MiniMapStyle"
                                     IsChecked="{Binding CurrentStyleKey, Converter={StaticResource EqualityConverter}, ConverterParameter=bright}" Click="StyleBtn_Click"/>
                        <RadioButton Content="🌑 Тёмная 3D" Tag="dark" Style="{StaticResource MapStyleRadio}" GroupName="MiniMapStyle"
                                     IsChecked="{Binding CurrentStyleKey, Converter={StaticResource EqualityConverter}, ConverterParameter=dark}" Click="StyleBtn_Click"/>
                        <RadioButton Content="🏝 Мультяшная" Tag="cartoon" Style="{StaticResource MapStyleRadio}" GroupName="MiniMapStyle"
                                     IsChecked="{Binding CurrentStyleKey, Converter={StaticResource EqualityConverter}, ConverterParameter=cartoon}" Click="StyleBtn_Click"/>
                        <RadioButton Content="☀ Светлая" Tag="light" Style="{StaticResource MapStyleRadio}" GroupName="MiniMapStyle"
                                     IsChecked="{Binding CurrentStyleKey, Converter={StaticResource EqualityConverter}, ConverterParameter=light}" Click="StyleBtn_Click"/>
                    </UniformGrid>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Coordinates tooltip -->
        <Border HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Margin="0,0,60,12"
                Style="{StaticResource Card}" Padding="8,5">
            <TextBlock x:Name="CoordLabel"
                       FontSize="11" FontFamily="{StaticResource MonoFont}"
                       Foreground="{StaticResource TextMutedBrush}"
                       Text="Нажмите на карту для добавления точки"/>
        </Border>
    </Grid>
</UserControl>
